{
	"dependencies": {
		"gray-matter": "^4.0.3",
		"marked": "^16.0.0"
	},
	"devDependencies": {
		"@nndvn/cfgs": "git+https://github.com/nndvn/cfgs.git",
		"@types/bun": "^1.2.18"
	},
	"module": "index.ts",
	"name": "hayhodotco.github.io",
	"peerDependencies": {
		"typescript": "^5.8.3"
	},
	"private": true,
	"scripts": {
		"build": "rm -rf dist && bun build --outdir=dist ./index.html",
		"dev": "bun ./index.html --console",
		"fix": "biome check --fix",
		"format": "biome format --fix",
		"lint": "biome lint --fix",
		"preview": "bun ./docs/**/*.html --console"
	},
	"type": "module"
}
